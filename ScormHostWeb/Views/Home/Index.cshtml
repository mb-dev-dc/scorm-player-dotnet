@model ScormHostWeb.Controllers.HomeIndexViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>

    @if (Model?.ShowLaunchCourse == true)
    {
        <p>Launch course <a href="@Model.LaunchCourseUrl">@Model.LaunchCourseText</a>.</p>
    }
</div>
